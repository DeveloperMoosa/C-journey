#include <stdio.h>

int main(){
// The different ways to code multiple   data types 
	int age = 20;
	float pi = 3.14;          //the value of pi
	char percentage = '%'; // the character will also want to be coded in single quotes

return 0;
}